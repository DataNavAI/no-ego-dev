# Prototype Starter

This deliberately incomplete static starter is copied into a unique eval-run workspace. Inspect `BENCHMARK.md`, then replace the placeholder with one dog-walker intake-to-handoff journey.

Run `python3 verify.py` after implementation. The initial starter should fail verification.
